# 1. Matikan fitur directory browsing untuk keamanan
Options -Indexes

# 2. Aktifkan modul rewrite Apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^$ public/ [L]
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>